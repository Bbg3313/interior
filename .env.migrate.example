# 이 파일을 복사해 .env.migrate 로 저장한 뒤 값을 채우세요 (Git에 올리지 않음)
# Supabase → Settings → API
# --- 예전 → 새 Supabase 전체 이전 (migrate:all) ---
# 예전 Interior Resume 후 Settings → API 의 service_role
# OLD_SUPABASE_URL=https://fvnxvpgwtoriewwtdphp.supabase.co
# OLD_SUPABASE_SERVICE_ROLE=
NEW_SUPABASE_URL=https://aqdmuqegokwecclcqhgc.supabase.co
NEW_SUPABASE_ANON_KEY=
NEW_SUPABASE_SERVICE_ROLE=
# Supabase → Settings → Database → Database password (프로젝트 만들 때 정한 비번)
NEW_SUPABASE_DB_PASSWORD=
